---
phase: 01-infrastructure-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - apps/core-api/prisma/seed.ts
autonomous: true
requirements:
  - INFRA-04

must_haves:
  truths:
    - "Seed script upserts all 6 categories by slug (idempotent — safe to run multiple times)"
    - "Seed creates one Buyer account (buyer@collectavo.dev) and one Seller account (seller@collectavo.dev) with bcrypt-hashed passwords"
    - "Seller account has a SellerProfile created via nested write"
    - "All 6 categories have 3–5 seeded Product records (18–30 products total)"
    - "Every seeded Product has at least 2 ProductImage records with picsum.photos CDN URLs"
    - "Product seed data uses realistic collectibles names and plausible prices (per D-06)"
    - "Seed script uses PrismaClient with driver adapter (PrismaPg) — not bare PrismaClient()"
    - "Pool.end() is called in .finally() to release connections after seed completes"
    - "bcrypt.hash() is called inside seed.ts at runtime — no pre-computed hash strings hardcoded"
    - "Seed uses upsert with stable unique keys — never deleteMany + create"
  artifacts:
    - path: "apps/core-api/prisma/seed.ts"
      provides: "Idempotent seed script with 6 categories, buyer/seller accounts, 18-30 listings with images"
      contains: "prisma.category.upsert"
  key_links:
    - from: "apps/core-api/prisma/seed.ts"
      to: "apps/core-api/src/generated/prisma/client"
      via: "import { PrismaClient } from '../src/generated/prisma/client'"
      pattern: "generated/prisma/client"
    - from: "apps/core-api/prisma/seed.ts"
      to: "@prisma/adapter-pg"
      via: "import { PrismaPg } from '@prisma/adapter-pg'"
      pattern: "PrismaPg"
    - from: "apps/core-api/prisma.config.ts"
      to: "apps/core-api/prisma/seed.ts"
      via: "migrations.seed: 'tsx prisma/seed.ts'"
      pattern: "tsx prisma/seed.ts"
---

<objective>
Write the complete idempotent seed script that populates all 6 product categories, buyer and seller accounts, and 18–30 realistic collectibles listings with images. The seed must use Prisma 7's required driver adapter pattern and bcrypt for password hashing.

Purpose: INFRA-04 requires seed data for all 6 product categories with representative sample listings. Phase 2 auth testing depends on the buyer/seller accounts. Phases 7 and 8 UI testing depend on realistic product data that makes the marketplace look real in demos (D-06).

Output: apps/core-api/prisma/seed.ts — a single complete seed file that runs via `npx prisma db seed` after migration.
</objective>

<execution_context>
@C:/ATS/Git/collectavo/.claude/get-shit-done/workflows/execute-plan.md
@C:/ATS/Git/collectavo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/ATS/Git/collectavo/.planning/PROJECT.md
@C:/ATS/Git/collectavo/.planning/ROADMAP.md
@C:/ATS/Git/collectavo/CLAUDE.md

<interfaces>
<!-- Patterns the executor MUST replicate exactly. Sourced from RESEARCH.md. -->

From RESEARCH.md Pattern 9 — Seed script structure:
- Import "dotenv/config" at top (seed runs outside NestJS DI)
- Import { PrismaPg } from "@prisma/adapter-pg"
- Import { Pool } from "pg"
- Import { PrismaClient } from "../src/generated/prisma/client" (relative from prisma/ directory)
- Instantiate: const pool = new Pool({ connectionString: process.env.DATABASE_URL }), const adapter = new PrismaPg(pool), const prisma = new PrismaClient({ adapter })
- main() function with all seed logic
- main().catch(console.error).finally(async () => { await pool.end(); })

CRITICAL — bcrypt requirement (from RESEARCH.md open questions + CONTEXT.md D-08):
- Call bcrypt.hash("Buyer123!", 10) and bcrypt.hash("Seller123!", 10) INSIDE main() function
- Do NOT hardcode pre-computed hash strings — they are environment-specific and fragile
- Add bcryptjs to core-api dependencies (bcryptjs is pure JS, no native compilation needed)
- Import: import * as bcrypt from 'bcryptjs'
- Add @types/bcryptjs to devDependencies

From CONTEXT.md decisions for seed data:
- D-05: 3–5 sample listings per category (18–30 total)
- D-06: Realistic data — actual product names and plausible prices
- D-07: Each listing has ProductImage records using https://picsum.photos/seed/{slug}/800/800
- D-08: buyer@collectavo.dev (password: Buyer123!) and seller@collectavo.dev (password: Seller123!)

From 01-SPECIFICS (in CONTEXT.md):
- Image URL pattern: https://picsum.photos/seed/{listing-slug}/800/800 — deterministic per listing
- Collectibles domain examples by category:
  * TCG: Pokémon (Charizard VMAX Rainbow Rare, Pikachu VMAX, Umbreon VMAX), One Piece, Yu-Gi-Oh
  * Funko: Specific Pop numbers and editions (e.g., "Goku #09 Entertainment Earth Exclusive")
  * Anime Figure: Scales with manufacturer (e.g., "Rem 1/7 Scale Figure — Good Smile Company")
  * Manga: Volume numbers with publisher (e.g., "One Piece Vol. 1 — VIZ Media First Print")
  * Retro Game: CIB/loose/complete status (e.g., "The Legend of Zelda: Ocarina of Time — CIB N64")
  * Limited Edition: Edition numbers (e.g., "Street Fighter 30th Anniversary 1/1000 Statue")

Category slugs (from RESEARCH.md Pattern 9):
- funko-pop, tcg, anime-figure, manga, limited-edition, retro-game

Category names:
- "Funko Pop", "Trading Card Game", "Anime Figure", "Manga", "Limited Edition", "Retro Game"

Enum values to use in seed (must match schema.prisma exactly):
- Role: BUYER, SELLER
- Condition: MINT, NEAR_MINT, EXCELLENT, GOOD, FAIR, POOR
- ListingType: DIRECT_SALE
- ProductStatus: ACTIVE
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Complete seed script with all 6 categories and realistic listings</name>
  <files>apps/core-api/prisma/seed.ts</files>
  <read_first>
    - C:/ATS/Git/collectavo/.planning/phases/01-infrastructure-foundation/01-RESEARCH.md (Pattern 9: full seed script structure — adapter pattern, Pool.end(), upsert idempotency rule; Pitfall 7: generated client not found — order matters; Anti-Patterns section: importing from @prisma/client is wrong)
    - C:/ATS/Git/collectavo/.planning/phases/01-infrastructure-foundation/01-CONTEXT.md (D-05 through D-08: seed data decisions; Specifics section for realistic product examples and image URL pattern)
    - C:/ATS/Git/collectavo/apps/core-api/package.json (created in Plan 01 — check if bcryptjs is present; if not, note it must be added to dependencies)
    - C:/ATS/Git/collectavo/apps/core-api/prisma/schema.prisma (created in Plan 02 — read all model field names and enum values before writing seed to ensure exact match)
  </read_first>
  <action>
    Create apps/core-api/prisma/seed.ts with all required seed logic. If bcryptjs is not in apps/core-api/package.json dependencies, add it (bcryptjs@^2.4.3) to dependencies and @types/bcryptjs@^2.4.4 to devDependencies in package.json before writing seed.ts.

    File structure for seed.ts:
    1. Import block: "dotenv/config", PrismaPg from @prisma/adapter-pg, Pool from pg, PrismaClient from ../src/generated/prisma/client, bcrypt (import * as bcrypt from 'bcryptjs' or import bcrypt from 'bcryptjs').
    2. Setup block: pool = new Pool({ connectionString: process.env.DATABASE_URL }), adapter = new PrismaPg(pool), prisma = new PrismaClient({ adapter }).
    3. main() async function containing all seed logic in this order:
       a. Categories — upsert all 6 by slug
       b. Look up each category by slug to get their IDs
       c. Hash passwords: buyerHash = await bcrypt.hash("Buyer123!", 10), sellerHash = await bcrypt.hash("Seller123!", 10)
       d. Buyer account — upsert by email with role: "BUYER", passwordHash: buyerHash
       e. Seller account — upsert by email with role: "SELLER", passwordHash: sellerHash. Use nested create for sellerProfile: { create: { displayName: "Demo Seller", bio: "Official Collectavo demo seller account." } } — but only on the create branch (update branch should be empty {} to avoid recreating on re-run)
       f. Per-category product seeding — for each category, create 3–5 products with upsert-by-title pattern. Each product must have: sellerId (seller.id), categoryId (the looked-up category id), title, description, price (as string for Decimal: "89.99"), condition (enum value), listingType: "DIRECT_SALE", status: "ACTIVE", metadata (JSON object per category type), createdAt offset to spread dates. Each product gets 2–3 ProductImage records created via nested create. Use picsum.photos/seed/{slug}/800/800 URL pattern where slug is a kebab-case version of the product title.

    Realistic product data by category (implement ALL — do not substitute generic names):
    
    TCG (5 listings): "Charizard VMAX Rainbow Rare #074 — Pokémon Sword & Shield" at $89.99 NEAR_MINT, "Pikachu VMAX Secret Rainbow Rare #188 — Pokémon Vivid Voltage" at $45.00 MINT, "Luffy Gear 5 OP-01 Secret Rare — One Piece TCG" at $67.50 NEAR_MINT, "Blue-Eyes White Dragon LOB-001 1st Edition" at $299.99 EXCELLENT, "Umbreon VMAX Alternate Art #215 — Pokémon Evolving Skies" at $199.99 NEAR_MINT. metadata for TCG: { set: "Sword & Shield", cardNumber: "074/073", rarity: "Secret Rare", edition: "Unlimited", language: "EN" } (vary per card).

    Funko Pop (4 listings): "Goku Super Saiyan #09 Entertainment Earth Exclusive" at $24.99 MINT, "Naruto Running #71 Hot Topic Exclusive" at $18.99 NEAR_MINT, "Iron Man Mark 85 Glow-in-Dark #529 GameStop Exclusive" at $34.99 MINT, "Pikachu with Pokéball #353 Special Edition" at $14.99 GOOD. metadata for Funko: { series: "Dragon Ball Z", edition: "Entertainment Earth Exclusive", exclusive: true, vaulted: false }.

    Anime Figure (3 listings): "Rem 1/7 Scale Figure — Good Smile Company Re:Zero" at $129.99 MINT, "Zero Two 1/7 Scale Figure — Kotobukiya DARLING in the FranXX" at $149.99 NEAR_MINT, "Mikasa Ackerman 1/8 Scale — Aniplex Attack on Titan" at $89.99 EXCELLENT. metadata: { scale: "1/7", manufacturer: "Good Smile Company", character: "Rem" }.

    Manga (4 listings): "One Piece Vol. 1 — VIZ Media First Edition" at $12.99 GOOD, "Demon Slayer Vol. 1-23 Complete Set — VIZ Media" at $149.99 NEAR_MINT, "Berserk Vol. 1 — Dark Horse Comics First Print" at $24.99 EXCELLENT, "Dragon Ball Z Vol. 1 Viz Big Edition — VIZ Media" at $19.99 NEAR_MINT. metadata: { volume: 1, publisher: "VIZ Media", language: "EN" }.

    Limited Edition (3 listings): "Street Fighter 30th Anniversary 1/1000 Polystone Statue — PCS" at $399.99 MINT, "Zelda: Tears of the Kingdom Collector's Edition Artbook" at $59.99 NEAR_MINT, "Pokémon 25th Anniversary Celebrations Ultra-Premium Collection" at $189.99 EXCELLENT. metadata: { editionSize: 1000, editionNumber: 247 }.

    Retro Game (4 listings): "The Legend of Zelda: Ocarina of Time — CIB N64" at $89.99 GOOD, "Super Mario Bros. / Duck Hunt — CIB NES" at $49.99 FAIR, "Final Fantasy VII — Complete PlayStation 1" at $79.99 EXCELLENT, "GoldenEye 007 — Cartridge Only N64" at $34.99 GOOD. metadata: { completeness: "CIB", hasBox: true, hasManual: true }.

    For ProductImage records: create 2 images per product using nested create in the product upsert. First image: isPrimary: true, sortOrder: 0. Second image: isPrimary: false, sortOrder: 1. URLs: https://picsum.photos/seed/{kebab-slug-of-title}/800/800 and https://picsum.photos/seed/{kebab-slug-of-title}-2/800/800.

    Idempotency note: Product upsert by title alone is not ideal (titles could theoretically repeat). Use upsert with where: { title: product.title } or — better — create a composite approach. Since products don't have a unique constraint on title, use a deleteMany({where: {seller: {email: 'seller@collectavo.dev'}}}) followed by createMany pattern ONLY for products (not for users or categories, which use upsert). This is acceptable because products owned by the demo seller are entirely recreatable without real user data loss.

    4. Entry point: main().catch(console.error).finally(async () => { await pool.end(); })
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const seed = fs.readFileSync('C:/ATS/Git/collectavo/apps/core-api/prisma/seed.ts', 'utf8'); const checks = ['dotenv/config','PrismaPg','pool.end()','bcrypt','Buyer123!','Seller123!','buyer@collectavo.dev','seller@collectavo.dev','prisma.category.upsert','picsum.photos','DIRECT_SALE','ACTIVE','sellerProfile','displayName','isPrimary','sortOrder','metadata']; checks.forEach(t => { if (!seed.includes(t)) throw new Error('seed.ts missing: ' + t); }); if (seed.includes('@prisma/client')) throw new Error('WRONG: seed imports from @prisma/client — must use generated path'); if (seed.includes('prisma-client-js')) throw new Error('WRONG: old provider reference in seed'); const catSlugs = ['funko-pop','tcg','anime-figure','manga','limited-edition','retro-game']; catSlugs.forEach(s => { if (!seed.includes(s)) throw new Error('seed.ts missing category slug: ' + s); }); console.log('seed.ts OK — all checks passed');"</automated>
  </verify>
  <acceptance_criteria>
    - apps/core-api/prisma/seed.ts exists
    - seed.ts imports from '../src/generated/prisma/client' (NOT from '@prisma/client')
    - seed.ts contains: pool = new Pool({ connectionString: process.env.DATABASE_URL }), PrismaPg(pool), PrismaClient({ adapter })
    - seed.ts contains pool.end() in .finally() block
    - seed.ts calls bcrypt.hash("Buyer123!", 10) and bcrypt.hash("Seller123!", 10) at runtime — no pre-computed hash strings
    - seed.ts upserts all 6 category slugs: funko-pop, tcg, anime-figure, manga, limited-edition, retro-game
    - seed.ts creates buyer@collectavo.dev and seller@collectavo.dev accounts
    - seed.ts creates SellerProfile with displayName for seller account
    - seed.ts creates products for all 6 categories with realistic names from the action spec (Charizard, Goku, Rem, One Piece, Street Fighter, Zelda Ocarina)
    - seed.ts creates ProductImage records with picsum.photos URLs and isPrimary/sortOrder fields
    - seed.ts includes metadata JSON per category matching the category's schema (set/cardNumber for TCG, scale/manufacturer for Anime, etc.)
    - If bcryptjs was not in package.json dependencies, it has been added there
  </acceptance_criteria>
  <done>seed.ts is complete and will produce 6 categories, 2 user accounts, 18-30 product listings with images when run via `npx prisma db seed`</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Seed script → PostgreSQL | Seed runs with full write access — must be idempotent to avoid data corruption on re-run |
| Hardcoded seed passwords → bcrypt | Seed passwords are dev-only; bcrypt factor 10 is appropriate for dev |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | Seed account passwords (Buyer123!, Seller123!) | accept | Dev-only accounts; passwords documented in .env.example comments; never used in production; seeded with bcrypt hash not plaintext |
| T-03-02 | Tampering | deleteMany on products before re-seed | accept | Only demo seller's products are deleted; real user data (if any) is not affected because this is a dev-only seed with a known seller email |
| T-03-03 | Repudiation | Seed account could be used in production accidentally | mitigate | Seed accounts use @collectavo.dev email domain not matching any real domain; documented as dev-only in README and .env.example |
| T-03-SC | Tampering | npm install of bcryptjs | mitigate | bcryptjs pre-audited: npm (2012, 13+ yrs), github.com/dcodeIO/bcrypt.js, pure JS implementation, no native compilation, no postinstall scripts |
</threat_model>

<verification>
After task completes:

1. Seed script syntax check:
   `docker compose exec core-api npx tsx prisma/seed.ts --dry-run` — will fail without DB but confirms TypeScript parses correctly

2. File content spot checks:
   - grep -c "prisma.category.upsert" apps/core-api/prisma/seed.ts → should be 6 (one per category)
   - grep -c "picsum.photos" apps/core-api/prisma/seed.ts → should be 36+ (2 per product, 18-30 products)
   - grep "pool.end()" apps/core-api/prisma/seed.ts → must exist exactly once

3. Import path sanity:
   - grep "from '@prisma/client'" apps/core-api/prisma/seed.ts → must return NO results
   - grep "generated/prisma/client" apps/core-api/prisma/seed.ts → must return 1 result
</verification>

<success_criteria>
- apps/core-api/prisma/seed.ts exists with complete seed logic
- Seed script uses Prisma 7 adapter pattern (PrismaPg + Pool)
- All 6 category slugs are seeded idempotently
- buyer@collectavo.dev and seller@collectavo.dev accounts seeded with bcrypt-hashed passwords
- 18–30 realistic collectibles listings spread across all 6 categories
- Every product has at least 2 ProductImage records with picsum.photos CDN URLs
- INFRA-04 requirement addressed: all 6 categories seeded with representative listings
</success_criteria>

<output>
Create `.planning/phases/01-infrastructure-foundation/01-03-SUMMARY.md` when done
</output>
