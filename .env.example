# .env.example — copy to .env and fill in real values
# This file is committed to source control. Never commit .env (the real one).

POSTGRES_USER=collectavo
POSTGRES_PASSWORD=change_me_dev
POSTGRES_DB=collectavo_dev

JWT_ACCESS_SECRET=change_me_min_32_chars_access
JWT_REFRESH_SECRET=change_me_min_32_chars_refresh

# Seeded dev accounts (created by: docker compose exec core-api npx prisma db seed)
# Seeded Seller: seller@collectavo.dev / Seller123!
# Seeded Buyer:  buyer@collectavo.dev  / Buyer123!
